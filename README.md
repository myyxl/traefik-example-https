# Traefik Example HTTPS

## What to change

- Change the domain in `docker-compose.yml` to which traefik should be available at
- Upload your certificate and key to `/certs`
- Update the certificate and key file name in `certs.yml`
